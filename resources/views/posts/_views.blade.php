<div class="inline-table lg:block flex mt-2">
    <div class="ml-1 mt-1">
        @if($views_count)
            <p>Viewed by {{$views_count}}</p>
        @else
            <p>No views yet</p>
        @endif
    </div>
</div>

